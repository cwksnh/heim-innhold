export default async function handler(req, res) {
  const response = await fetch("https://api.anthropic.com/v1/messages", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "x-api-key": process.env.ANTHROPIC_API_KEY,
      "anthropic-version": "2023-06-01"
    },
    body: JSON.stringify(req.body),
  });
  const data = await response.json();
  res.status(200).json(data);
}
```

Lagre med **Ctrl+S**.

---

**Steg 2 — Oppdater App.js**

Åpne `App.js` i Notepad igjen. Finn denne linjen:
```
const response = await fetch("https://api.anthropic.com/v1/messages", {
```

Bytt den ut med:
```
const response = await fetch("/api/generate", {
```

Fjern også disse to linjene rett under, siden API-nøkkelen nå håndteres av serveren:
```
"x-api-key": process.env.REACT_APP_ANTHROPIC_API_KEY,
"anthropic-version": "2023-06-01"
```

Lagre.

---

**Steg 3 — Skaff en API-nøkkel**

Gå til [console.anthropic.com](https://console.anthropic.com), lag en konto og klikk **«API Keys»** → **«Create Key»**. Kopier nøkkelen og lagre den et trygt sted — du ser den bare én gang.

---

**Steg 4 — Legg koden på GitHub**

Gå til [github.com](https://github.com) og lag en gratis konto. Klikk **«New repository»**, kall det `heim-innhold`, og klikk **«Create repository»**.

GitHub viser deg noen kommandoer. Gå tilbake til Command Prompt og lim inn disse én etter én:
```
git init
git add .
git commit -m "første versjon"
git branch -M main
git remote add origin [lenken GitHub viser deg]
git push -u origin main